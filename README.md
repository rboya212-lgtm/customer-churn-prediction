# customer-churn-prediction
Customer Churn Prediction CLI Tool is an end-to-end Machine Learning project in Python. It processes data, trains a Random Forest model, and generates batch predictions directly via terminal commands without a GUI.
